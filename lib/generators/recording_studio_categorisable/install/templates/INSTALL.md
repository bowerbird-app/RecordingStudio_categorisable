RecordingStudioCategorisable install complete.

Next steps:

1. Review `config/initializers/recording_studio_categorisable.rb`.
2. Wire a `root_recording_resolver` for the mounted UI.
3. Register categorisable recordables explicitly.
4. Run `bin/rails tailwindcss:build` if you use Tailwind CSS.
5. Visit the configured mount path and create your first category group.
