RecordingStudioCategorisable install complete.

Next steps:

1. Review config/initializers/recording_studio_categorisable.rb and set any required options.
2. If you use environment-specific settings, create config/recording_studio_categorisable.yml.
3. Run bin/rails tailwindcss:build if you use Tailwind CSS.
4. Mount routes are added at the configured mount path. Adjust auth, layout, and current actor integration to match your host app.