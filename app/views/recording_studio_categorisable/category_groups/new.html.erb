<div class="mx-auto w-full max-w-4xl px-6 py-12">
  <%= render FlatPack::PageTitle::Component.new(
    title: "New category group",
    subtitle: "Create a reusable category group anywhere in the current recording tree.",
    variant: :h1
  ) %>

  <%= render "form", category_group: @category_group, submit_text: "Create category group" %>
</div>
