<div class="mx-auto w-full max-w-4xl px-6 py-12">
  <%= render FlatPack::PageTitle::Component.new(
    title: "Edit category group",
    subtitle: "Revises the current category group snapshot and keeps its item recordings attached.",
    variant: :h1
  ) %>

  <%= render "form", category_group: @category_group, submit_text: "Save category group" %>
</div>
